# VoxHorizon — VPS container stack.
#
# Topology: Caddy → web → worker. Only Caddy has public ports (:80/:443);
# web (Next.js, :3000) and worker (FastAPI, :8000) live on the internal
# Docker network and are reached by Caddy via service DNS. Worker stays
# `expose:`-only — no host port binding — so the host firewall cannot
# accidentally surface it.
#
# Caddy fronts the dashboard over HTTPS (Let's Encrypt) and terminates TLS.
# The web service proxies API calls to worker over the internal network;
# Caddy itself only speaks to web.

# Default docker json-file log rotation, applied to every service via the
# `*default-logging` anchor below. Without this the json-file driver grows
# unbounded; a chatty service (e.g. Caddy's per-SSE-reconnect reverse_proxy
# warnings) can fill the host disk and wedge the box (incl. sshd). 10m x 3 caps
# each container at ~30MB of logs.
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    image: ghcr.io/${GHCR_OWNER:-mogginglabs}/voxhorizon-worker:${WORKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    logging: *default-logging
    # Production path: env file lives at the canonical absolute path on
    # the VPS (chmod 600, owned by the deploy user). Dev override:
    #   env_file: ./worker/.env
    # — substitute the line below when running locally on a workstation.
    env_file:
      - /opt/voxhorizon/.env
    expose:
      - "8000"
    # Hermes bridge (HI-5, #247): the worker exec_runs into the co-located
    # Hermes container (Ekko runtime) via the host's Docker socket. Names
    # are sourced from env so dev/prod can target different containers.
    environment:
      HERMES_CONTAINER_NAME: ${HERMES_CONTAINER_NAME:-hermes-agent-ekko}
      DASHBOARD_WEBHOOK_TOKEN: ${DASHBOARD_WEBHOOK_TOKEN}
    volumes:
      # B-roll pool — Diogo curates this over time; persistent named
      # volume keeps it across image rebuilds.
      - worker-broll-pool:/app/storage/broll-pool
      # Worker logs / runtime state. The image logs to stdout (JSON via
      # structlog), but anything written under /app/storage (sidecars,
      # tmp transcode buffers) lives here.
      - worker-logs:/app/storage/logs
      # Host Docker socket — rw, required for the Hermes bridge to
      # exec_run into hermes-agent-ekko. SECURITY: this is effectively
      # root-equivalent access on the host; the worker is co-located
      # with Ekko by design (already trusted neighbors), all worker
      # routes are bearer-authed, and the worker has no untrusted code
      # path. See SECRETS.md (HI-12) for the full threat model.
      - /var/run/docker.sock:/var/run/docker.sock
    # The `app` user inside the worker image (uid 1001) needs to be a
    # member of the host's docker group to read /var/run/docker.sock.
    # `group_add` injects the supplementary GID into the container's
    # process at runtime — no Dockerfile change required. Default 988
    # matches the Hostinger VPS probes; override via DOCKER_GID in
    # /opt/voxhorizon/.env if the host's docker group has a different
    # GID (`getent group docker`).
    group_add:
      - "${DOCKER_GID:-988}"
    # Multi-network: the worker has to be reachable from the Hermes
    # container (which runs in its own Hostinger-managed compose stack,
    # network `hermes-agent-ekko_default`). The Hermes-side plugin
    # (voxhorizon-approvals) POSTs to http://worker:8000 — that
    # hostname has to resolve from inside ekko's network. We attach the
    # worker to the external Hermes network with `worker` as a network
    # alias, leaving the dashboard's web ↔ worker traffic on the
    # default voxhorizon network unchanged.
    networks:
      default: {}
      hermes-agent-ekko_default:
        aliases:
          - worker
    healthcheck:
      test:
        - CMD-SHELL
        - 'curl -fsS -H "Authorization: Bearer ${WORKER_SHARED_SECRET}" http://localhost:8000/work/health || exit 1'
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3

  # operator-daemon: silent-failure foundational redesign PR-3.
  #   Sidecar container that owns the work_item queue's operator_dispatch
  #   kind. The daemon claims queued operator_dispatch rows from the worker's
  #   REST surface and docker-execs into the co-located hermes-agent-operator
  #   container (NEVER mutates the operator image's CMD -- the daemon is a
  #   SIDECAR, not a swap). Health is exposed on :9001/healthz so the docker
  #   healthcheck + the dashboard's DaemonHealthBadge can both probe it.
  operator-daemon:
    build:
      context: ./operator-daemon
      dockerfile: Dockerfile
    image: ghcr.io/${GHCR_OWNER:-mogginglabs}/voxhorizon-operator-daemon:${OPERATOR_DAEMON_IMAGE_TAG:-latest}
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - /opt/voxhorizon/.env
    expose:
      - "9001"
    environment:
      # The daemon docker-execs into this container to drive the operator. The
      # name lives in env so dev/prod (and per-VPS overrides) can target a
      # different operator container without a compose edit.
      HERMES_OPERATOR_CONTAINER_NAME: ${HERMES_OPERATOR_CONTAINER_NAME:-hermes-agent-operator}
    volumes:
      # Host docker socket -- required for the daemon to docker-exec into the
      # operator container. Same threat model as the worker mount above; see
      # SECRETS.md (HI-12).
      - /var/run/docker.sock:/var/run/docker.sock
    # The `app` user (uid 10001 in the daemon image) needs the host docker
    # group's GID to read /var/run/docker.sock. Mirrors the worker.
    group_add:
      - "${DOCKER_GID:-988}"
    # Multi-network: the daemon POSTs to the worker's REST surface at
    # http://worker:8000 over the default voxhorizon network AND has to reach
    # the operator container which lives on the Hermes-managed stack's
    # network (mirrors the worker's attachment pattern).
    networks:
      default: {}
      hermes-agent-ekko_default: {}
    depends_on:
      # Don't try to claim work before the worker's /work/queue/claim endpoint
      # is reachable -- the daemon would crash-loop on connection refused.
      worker:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - "curl -fsS http://localhost:9001/healthz || exit 1"
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3

  # web: VPS-8 (#229)
  #   Next.js dashboard. Builds NEXT_PUBLIC_* env into the static bundle at
  #   image-build time (they're inlined into client JS, so they must be
  #   present as build args, not just runtime env). Runtime secrets come
  #   from the shared /opt/voxhorizon/.env. No public port — Caddy reaches
  #   it on the internal Docker network as `web:3000`.
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:-}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}
        NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${NEXT_PUBLIC_VAPID_PUBLIC_KEY:-}
    image: ghcr.io/${GHCR_OWNER:-mogginglabs}/voxhorizon-web:${WEB_IMAGE_TAG:-latest}
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - /opt/voxhorizon/.env
    expose:
      - "3000"
    depends_on:
      worker:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - "curl -fsS http://localhost:3000/ || exit 1"
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3

  # caddy: VPS-3 (#160), updated for VPS-8 (#229)
  #   Fronts the dashboard (web) over HTTPS (Let's Encrypt), terminates
  #   TLS, and exposes :80 + :443 publicly. Caddy now talks to `web`, not
  #   `worker` — the worker is reached only through the web app's server
  #   routes on the internal network.
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    logging: *default-logging
    depends_on:
      web:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp" # HTTP/3
    # Read env from the VPS .env directly. Critical: the bcrypt hash for
    # the basic-auth gate contains `$` characters; if we passed it
    # through compose's ${VAR} interpolation, every `$<name>` segment in
    # the hash (e.g. `$DUz...`) would be mistaken for a variable
    # reference and substituted to empty, producing a truncated /
    # invalid hash inside the container. env_file injects values
    # verbatim, no interpolation pass.
    env_file:
      - /opt/voxhorizon/.env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data # ACME certs
      - caddy-config:/config # Caddy state
      - caddy-logs:/var/log/caddy

volumes:
  worker-broll-pool:
    name: voxhorizon-worker-broll-pool
  worker-logs:
    name: voxhorizon-worker-logs
  caddy-data:
    name: voxhorizon-caddy-data
  caddy-config:
    name: voxhorizon-caddy-config
  caddy-logs:
    name: voxhorizon-caddy-logs

networks:
  # Implicit default — keeps web, caddy, worker on a private bridge.
  default:
    name: voxhorizon_default
  # Hostinger's Hermes-Ekko compose stack owns this network. We attach
  # the worker as an external participant (with alias `worker`) so the
  # in-container voxhorizon-approvals plugin can POST to the long-poll
  # endpoint by service-name. Marked external so compose does not try
  # to manage its lifecycle.
  hermes-agent-ekko_default:
    external: true
